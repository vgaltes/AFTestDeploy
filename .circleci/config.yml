version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.10

    working_directory: ~/repo

    steps:
      - checkout
      - run:
          name: Install azure functions core tools
          command: npm install azure-functions-core-tools
      - run:
          name: Test azure functions core tools
          command: func
